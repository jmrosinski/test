#include <stdio.h>
#include "gptl.h"

int GPTLstart (int arg)
{
  printf ("GPTLstart: input arg=%d\n", arg);
  return 0;
}

int GPTLstop (int arg)
{
  printf ("GPTLstop: input arg=%d\n", arg);
  return 0;
}
